## MLflow (placeholder)

Later, this folder will contain the artifacts needed to run MLflow Tracking + Model Registry locally and/or in AWS (ECS on EC2 behind an ALB).

Not included in Step 1:
- docker compose / ECS task definitions
- database/object-store wiring
- authentication/authorization
